# 정서샤워 수동 테스트 체크리스트

## Phase 3-1: Auth Migration 검증

### 사전 준비
- [ ] Supabase Dashboard 접속: https://supabase.com/dashboard/project/rkymwqoiqqszpvblwtsx
- [ ] 로컬 개발 서버 실행: `npm run dev`
- [ ] 브라우저 개발자 도구 열기 (F12)

---

## 1. Supabase 데이터베이스 확인

### 테이블 생성 확인
- [ ] Supabase Dashboard → Table Editor
- [ ] `users` 테이블 존재 확인
- [ ] `user_cohorts` 테이블 존재 확인
- [ ] `cohorts` 테이블 존재 확인
- [ ] `challenges` 테이블 존재 확인
- [ ] `daily_records` 테이블 존재 확인
- [ ] `surveys` 테이블 존재 확인
- [ ] `community_posts` 테이블 존재 확인
- [ ] `community_comments` 테이블 존재 확인
- [ ] `applications` 테이블 존재 확인

### RLS 정책 확인
- [ ] Authentication → Policies
- [ ] 각 테이블에 RLS 정책이 활성화되어 있는지 확인

---

## 2. 회원가입 테스트

### 정상 회원가입 플로우
1. [ ] `/intro` 페이지 접속
2. [ ] "시작하기" 버튼 클릭 → `/signup`으로 이동
3. [ ] 폼 입력:
   - 이름: `테스트유저`
   - 이메일: `test1@test.com` (매번 새로운 이메일 사용)
   - 비밀번호: `test123`
   - 비밀번호 확인: `test123`
   - 인증 코드: `TEST01`
4. [ ] "회원가입" 버튼 클릭
5. [ ] 로딩 상태 표시 확인 ("처리 중...")
6. [ ] 에러 없이 `/home`으로 리다이렉트 확인

### Supabase Dashboard 확인
7. [ ] Authentication → Users에서 새 사용자 생성 확인
8. [ ] Table Editor → `users` 테이블에서 프로필 생성 확인
   - `name`: `테스트유저`
   - `email`: `test1@test.com`
   - `current_cohort_id`: 값 존재 확인
9. [ ] Table Editor → `user_cohorts`에서 참여 기록 확인
   - `user_id`: 방금 생성된 사용자 ID
   - `cohort_id`: TEST01 코드에 연결된 cohort ID
   - `status`: `active`

### 유효성 검증 테스트
10. [ ] 이메일 형식 오류 테스트: `invalid-email` 입력 → "올바른 이메일 형식이 아닙니다" 확인
11. [ ] 비밀번호 불일치 테스트: 다른 비밀번호 입력 → "비밀번호가 일치하지 않습니다" 확인
12. [ ] 짧은 비밀번호 테스트: `123` 입력 → "비밀번호는 최소 6자 이상이어야 합니다" 확인
13. [ ] 잘못된 코드 테스트: `WRONG1` 입력 → "유효하지 않거나 이미 사용된 코드입니다" 확인

### 중복 회원가입 테스트
14. [ ] 동일한 이메일로 다시 회원가입 시도
15. [ ] "이미 가입된 이메일입니다" 에러 메시지 확인

---

## 3. 로그인 테스트

### 정상 로그인 플로우
1. [ ] 로그아웃 (Profile 페이지에서)
2. [ ] `/login` 페이지 접속
3. [ ] 폼 입력:
   - 이메일: `test1@test.com` (위에서 생성한 계정)
   - 비밀번호: `test123`
4. [ ] "로그인" 버튼 클릭
5. [ ] 로딩 상태 확인
6. [ ] `/home`으로 리다이렉트 확인
7. [ ] 사용자 정보 정상 표시 확인

### Enter 키 테스트
8. [ ] 로그아웃 후 `/login`으로 이동
9. [ ] 이메일, 비밀번호 입력 후 Enter 키 누르기
10. [ ] 정상 로그인 확인

### 잘못된 로그인 테스트
11. [ ] 존재하지 않는 이메일로 로그인 시도
12. [ ] "이메일 또는 비밀번호가 일치하지 않습니다" 확인
13. [ ] 잘못된 비밀번호로 로그인 시도
14. [ ] 동일한 에러 메시지 확인

---

## 4. 세션 지속성 테스트

### 새로고침 테스트
1. [ ] 로그인 상태에서 `/home` 페이지
2. [ ] 브라우저 새로고침 (F5)
3. [ ] 로딩 화면 ("로딩 중...") 잠깐 표시 확인
4. [ ] 로그인 상태 유지 확인

### 탭 간 세션 공유 테스트
5. [ ] 로그인된 상태에서 새 탭 열기
6. [ ] 새 탭에서 `/home` 접속
7. [ ] 자동으로 로그인 상태 확인

### LocalStorage 확인
8. [ ] 개발자 도구 → Application → Local Storage
9. [ ] Supabase auth 토큰 저장 확인
10. [ ] `hasCompletedOnboarding` 값 확인

---

## 5. 로그아웃 테스트

### 정상 로그아웃
1. [ ] `/profile` 페이지 접속
2. [ ] "로그아웃" 버튼 클릭
3. [ ] `/intro` 또는 로그인 페이지로 리다이렉트 확인
4. [ ] 보호된 페이지 (`/home`, `/community`) 접근 시 `/unauthorized`로 리다이렉트 확인

### Supabase Auth 세션 확인
5. [ ] 개발자 도구 → Application → Local Storage
6. [ ] Supabase auth 토큰 삭제 확인

---

## 6. Auth State Listener 테스트

### 세션 만료 시뮬레이션
1. [ ] 로그인 상태 유지
2. [ ] 개발자 도구 → Console
3. [ ] "Auth state changed" 로그 확인 (앱 로드 시)
4. [ ] Supabase Dashboard → Authentication → Users → 사용자 선택 → "Sign out user"
5. [ ] 브라우저에서 자동으로 로그아웃 처리 확인 (새로고침 필요 없음)

### 다중 탭 로그아웃 테스트
6. [ ] 두 개의 탭에서 로그인 상태
7. [ ] 한 탭에서 로그아웃
8. [ ] 다른 탭에서도 자동으로 로그아웃 확인

---

## 7. 초기 로딩 상태 테스트

### Cold Start (처음 방문)
1. [ ] 브라우저 캐시 및 LocalStorage 완전 삭제
2. [ ] `http://localhost:5173` 접속
3. [ ] Splash 화면 표시 확인
4. [ ] Splash 완료 후 "로딩 중..." 화면 잠깐 표시 확인
5. [ ] `/intro` 페이지 표시 확인

### Warm Start (재방문, 로그인 상태)
6. [ ] 로그인 후 브라우저 새로고침
7. [ ] Splash 화면 표시
8. [ ] "로딩 중..." 화면 잠깐 표시
9. [ ] `/home` 페이지 표시 (로그인 상태 복원)

---

## 8. 에러 핸들링 테스트

### 네트워크 오류 시뮬레이션
1. [ ] 개발자 도구 → Network → Offline 체크
2. [ ] 로그인 시도
3. [ ] 적절한 에러 메시지 표시 확인
4. [ ] 네트워크 복구 후 정상 동작 확인

### 부분 실패 시나리오
5. [ ] 회원가입 중 프로필 생성 실패 시:
   - "프로필 생성에 실패했습니다. 이미 가입이 완료되었을 수 있습니다. 로그인을 시도해보세요." 메시지 확인
6. [ ] 회원가입 중 기수 참여 실패 시:
   - "기수 참여 등록에 실패했습니다. 계정은 생성되었으니 로그인 후 다시 시도해주세요." 메시지 확인

---

## 9. 콘솔 에러 확인

### 정상 동작 시
- [ ] 브라우저 콘솔에 예상치 못한 에러가 없는지 확인
- [ ] "Auth state changed" 로그가 적절히 출력되는지 확인
- [ ] "Session token refreshed" 로그 확인 (1시간 후)

### 에러 발생 시
- [ ] 모든 에러가 try-catch로 잡히는지 확인
- [ ] 에러 메시지가 사용자에게 적절히 표시되는지 확인

---

## 10. 보안 확인

### 환경 변수
- [ ] `.env.local` 파일이 git에 커밋되지 않았는지 확인
- [ ] `git status`로 .env.local이 "Ignored files"에 있는지 확인
- [ ] `.env.example`만 git에 추가되어 있는지 확인

### RLS 정책
- [ ] Supabase Dashboard에서 각 테이블의 RLS가 활성화되어 있는지 확인
- [ ] 다른 사용자의 데이터에 접근 시도 → 접근 거부 확인

---

## 테스트 결과 기록

### 발견된 이슈

| 이슈 번호 | 설명 | 심각도 | 상태 |
|----------|------|--------|------|
| 1 |  | 🔴/🟡/🟢 | ⏳/✅ |
| 2 |  | 🔴/🟡/🟢 | ⏳/✅ |

### 테스트 완료 시간
- 시작: _______________
- 완료: _______________
- 소요 시간: _______________

### 테스터 서명
- 이름: _______________
- 날짜: _______________

---

## 다음 단계

Auth 마이그레이션 테스트 완료 후:
- [ ] 발견된 이슈 수정
- [ ] 다음 Store 마이그레이션 진행 (challengeStore, dailyRecordStore 등)
